interface:
  display_name: "Delegate Codex Agent"
  short_description: "Route bounded tasks with isolated context"
  default_prompt: "Use $delegate-codex-agent to route this bounded task to Luna, Terra, or an isolated Sol thinking delegate and supervise its result."
